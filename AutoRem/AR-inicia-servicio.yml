---
- hosts: ntpocrh8005
  tasks:
  - name: revisa proceso
    shell: echo Inicia-Servicio
    register: result_valid
  - debug: var=result_valid.stdout_lines
  
  - name: Inicializa Servicio
    ansible.builtin.wait_for:
        timeout: 60
    delegate_to: localhost